需要改进：
目前似乎是输入 ticks 以运行，但我想改造为一开始运行就不停下来的，这样才符合演化。
没有说演化1000年的，而是一旦开始就永不停息。而由于当前还不是完全体，
因此需要有暂停和重置两个功能。

需要添加的功能或计划：
1. 暂停功能，需要保存快照，目的是在程序需要关闭时保存当前状态，下一次直接从该状态运行。
2. 重置功能，作为测试阶段的有效手段。
测试完当前版本后，下个版本需要从头开始演化，不再使用上版本的测试数据。
3. 时间流速控制。可设置时间流速，方便用户观察。
但是 CLI 模式下在保证程序运行的同时如何更改时间流速呢，或许可以考虑多线程，
但如果太过复杂，也可以保存当前快照后暂停运行，更改时间流速后再运行，
这样的话就必须有配置文件来控制这些全局属性。另外，还要设置基础速率，
比如一个 tick 是几年或者千年、万年
4. 最后的总结，目前只是简单的事件堆砌，我想要的是更智能化一点的。
比如：在本次模拟中，发生了什么，什么进化了，演化发展到哪一步了，亦或是灭绝了，等等内容
这样有需要有一个较复杂的事件总结系统了吧，总不能我搞一个Agent过来吧
5. 中文，中文，还是中文！你要我看一堆拉丁语系的生僻单词吗！其他文档里面那些英文也给我改了，
比如 README
6. 每一次运行的日志是latest.json对吧，能不能不要用json了，用户不友好，得一直翻
我的想法是，把每个tick的所有属性都要记录下来，可以观察变化，在重要事件发生时更是要记录。
控制台中，不能只有事件消息，还需要时刻有关键参数的打印，可以观察变化。
但转念一想，这样的话不是做成GUI更方便？只需要在一个框里不断更新数字就好了，
放在控制台还要打印不知道多少东西。做出GUI，有一个面板，平时就显示主要参数，
展开可以显示全部参数，然后主界面打印重要事件消息，右侧显示重要事件发生时的所有参数。
7. 我把参数换成5000，为什么tick是从4000开始的？不应该从0开始到5000吗，
这本质上还是运行了1000次，要么下一步就直接改为永久运行，要么就是先改为有限时间运行，
但是要把时间给我搞对了，不能写5000只运行1000个tick
8. tick开始的时候要有消息，xx开始演化之类的
9. "一个谱系现已占据主导" "一个或多个RNA家族退化至灭绝"，把具体哪些家族列出来
10. 为什么感觉这次我这个甲方需求这么多，也是体会到实际开发中甲方变需求的压力了好吧，
我们做的另一个课设项目、实验项目，甚至于简历项目，我不怎么变需求，只是让你们规划、生成、
测试，就好了，这一次似乎我总是想改这改那